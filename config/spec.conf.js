const specConf = {
    specs: [
        './tests/mobile/features/*.feature'
    ],
    // Patterns to exclude.
    exclude: [
        // 'path/to/excluded/files'
    ]
}

module.exports = { specConf }